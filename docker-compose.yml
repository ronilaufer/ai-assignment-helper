services:
  backend:
    build: ./backend
    container_name: ai_helper_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /app/venv

  frontend:
    build: ./frontend
    container_name: ai_helper_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
